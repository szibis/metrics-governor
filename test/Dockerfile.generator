FROM golang:1.25.7-alpine AS builder

WORKDIR /app

# Copy and build the generator
COPY generator.go go.mod go.sum ./
RUN go mod download && go build -o /generator generator.go

FROM alpine:3.21

RUN apk --no-cache add ca-certificates

COPY --from=builder /generator /generator

ENTRYPOINT ["/generator"]
