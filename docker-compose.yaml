version: "3.8"

services:
  # metrics-governor - the main proxy
  metrics-governor:
    build: .
    ports:
      - "4317:4317"   # gRPC receiver
      - "4318:4318"   # HTTP receiver
      - "9090:9090"   # Prometheus metrics
    command:
      - "-exporter-endpoint=otel-collector:4317"
      - "-exporter-insecure=true"
      - "-stats-labels=service,env"
      - "-limits-config=/etc/metrics-governor/limits.yaml"
      - "-limits-dry-run=false"
    volumes:
      - ./examples/limits.yaml:/etc/metrics-governor/limits.yaml:ro
    depends_on:
      - otel-collector

  # OpenTelemetry Collector as the backend
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.92.0
    ports:
      - "14317:4317"   # OTLP gRPC (external)
      - "14318:4318"   # OTLP HTTP (external)
    volumes:
      - ./test/otel-collector-config.yaml:/etc/otelcol/config.yaml:ro
    command: ["--config=/etc/otelcol/config.yaml"]
    depends_on:
      - victoriametrics

  # VictoriaMetrics for metrics storage (accepts OTLP directly)
  victoriametrics:
    image: victoriametrics/victoria-metrics:v1.96.0
    ports:
      - "8428:8428"    # HTTP API and UI
      - "8089:8089"    # OpenTelemetry ingestion
    command:
      - "--storageDataPath=/victoria-metrics-data"
      - "--httpListenAddr=:8428"
      - "--opentsdbHTTPListenAddr=:8089"
      - "--retentionPeriod=1d"
      - "--search.latencyOffset=0s"

  # Test metrics generator
  metrics-generator:
    build:
      context: ./test
      dockerfile: Dockerfile.generator
    environment:
      - OTLP_ENDPOINT=metrics-governor:4317
      - METRICS_INTERVAL=1s
      - SERVICES=payment-api,order-api,inventory-api,legacy-app
      - ENVIRONMENTS=prod,staging,dev
      - ENABLE_EDGE_CASES=true
      - ENABLE_HIGH_CARDINALITY=true
      - ENABLE_BURST_TRAFFIC=true
      - HIGH_CARDINALITY_COUNT=100
      - BURST_SIZE=500
      - BURST_INTERVAL_SEC=30
    depends_on:
      - metrics-governor
